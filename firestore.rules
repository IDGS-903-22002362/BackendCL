rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Todo el acceso a datos pasa por Cloud Functions (Admin SDK).
    // Clientes web/móvil no deben leer ni escribir Firestore directamente.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}